---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.base.json
  - tsconfig.json
  - vitest.config.ts
  - .gitignore
  - .prettierignore
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/engine/package.json
  - packages/engine/tsconfig.json
  - packages/engine/src/index.ts
  - packages/storage/package.json
  - packages/storage/tsconfig.json
  - packages/storage/src/index.ts
  - packages/python-sidecar/requirements.txt
  - packages/python-sidecar/sandbox_runner.py
  - apps/console/package.json
  - apps/console/tsconfig.json
  - apps/console/tsconfig.node.json
  - apps/console/index.html
  - apps/console/src/main.tsx
  - apps/console/src/App.tsx
  - apps/console/src/vite-env.d.ts
autonomous: true

must_haves:
  truths:
    - 'npm install at repo root succeeds and creates node_modules/@trajectory/ symlinks for server, engine, storage, console'
    - 'tsc --build compiles all four TypeScript packages without errors'
    - 'vitest run discovers projects for server, engine, storage, console (even if no tests exist yet)'
    - 'Each package tsconfig extends tsconfig.base.json and has correct module resolution (NodeNext for packages, Bundler for console)'
  artifacts:
    - path: 'package.json'
      provides: 'Root workspace config with workspaces array'
      contains: 'workspaces'
    - path: 'tsconfig.base.json'
      provides: 'Shared TypeScript base with strict: true'
      contains: 'strict'
    - path: 'tsconfig.json'
      provides: 'Root project references for tsc --build'
      contains: 'references'
    - path: 'vitest.config.ts'
      provides: 'Workspace-wide Vitest config'
      contains: 'projects'
    - path: 'packages/server/package.json'
      provides: '@trajectory/server package'
      contains: '@trajectory/server'
    - path: 'packages/engine/package.json'
      provides: '@trajectory/engine package'
      contains: '@trajectory/engine'
    - path: 'packages/storage/package.json'
      provides: '@trajectory/storage package'
      contains: '@trajectory/storage'
    - path: 'apps/console/package.json'
      provides: '@trajectory/console package'
      contains: '@trajectory/console'
  key_links:
    - from: 'packages/server/package.json'
      to: '@trajectory/engine'
      via: 'workspace dependency'
      pattern: '"@trajectory/engine"'
    - from: 'packages/engine/package.json'
      to: '@trajectory/storage'
      via: 'workspace dependency'
      pattern: '"@trajectory/storage"'
    - from: 'packages/server/tsconfig.json'
      to: 'packages/engine'
      via: 'TypeScript project reference'
      pattern: '"path": "../engine"'
    - from: 'packages/engine/tsconfig.json'
      to: 'packages/storage'
      via: 'TypeScript project reference'
      pattern: '"path": "../storage"'
---

<objective>
Scaffold the npm workspaces monorepo with all four TypeScript packages, shared TypeScript configuration, Vitest workspace config, and the Python sidecar directory. After this plan, `npm install` links all packages, `tsc --build` compiles cleanly, and `vitest run` discovers all projects.

Purpose: Establish the foundational directory structure and build configuration that every subsequent phase builds on. Without this, no package can import another and no builds or tests can run.
Output: Root workspace config, four packages with tsconfigs and stub source files, shared tsconfig.base.json, root tsconfig.json with project references, vitest.config.ts, Python sidecar placeholder, .gitignore.
</objective>

<execution_context>
@C:\Users\dnbra\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dnbra\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root workspace config and shared TypeScript base</name>
  <files>
    package.json
    tsconfig.base.json
    tsconfig.json
    vitest.config.ts
    .gitignore
    .prettierignore
  </files>
  <action>
Create the root `package.json` with these exact settings:
- `"name": "Trajectory-action-container"`
- `"version": "0.0.1"`
- `"private": true`
- `"type": "module"`
- `"workspaces": ["packages/*", "apps/*"]` — do NOT include `packages/python-sidecar` since it has no package.json
- `"scripts"` section with: `"build": "tsc --build"`, `"test": "vitest run"`, `"test:watch": "vitest"` — leave dev/lint/format scripts for Plan 02
- `"devDependencies"` with: `typescript` (^5.0.0), `vitest` (^4.0.0), `@vitest/coverage-v8` (latest)
- `"engines": { "node": ">=20.0.0" }`

Create `tsconfig.base.json` at root with shared compiler options:

```json
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "verbatimModuleSyntax": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

Create root `tsconfig.json` with project references only (no compilerOptions):

```json
{
  "files": [],
  "references": [
    { "path": "./packages/storage" },
    { "path": "./packages/engine" },
    { "path": "./packages/server" },
    { "path": "./apps/console" }
  ]
}
```

Order matters: storage before engine before server (dependency order for `tsc --build`).

Create root `vitest.config.ts`:

```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    projects: ['packages/*', 'apps/*'],
  },
})
```

Create `.gitignore` with entries for: `node_modules/`, `dist/`, `*.tsbuildinfo`, `.env`, `.env.*`, `coverage/`, `.vite/`, `*.db`, `*.db-journal`, `*.db-wal`, `__pycache__/`, `*.pyc`. Also ignore the existing temp files (`~$*`).

Create `.prettierignore` with entries for: `dist/`, `node_modules/`, `coverage/`, `*.db`, `packages/python-sidecar/`.
</action>
<verify>
Validate file existence and JSON syntax:

```bash
node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"
node -e "JSON.parse(require('fs').readFileSync('tsconfig.base.json','utf8'))"
node -e "JSON.parse(require('fs').readFileSync('tsconfig.json','utf8'))"
```

Verify tsconfig.base.json contains `"strict": true` and `"module": "NodeNext"`.
Verify root tsconfig.json has 4 references.
</verify>
<done>
Root package.json declares workspaces for packages/_ and apps/_. Shared tsconfig.base.json has strict TypeScript with NodeNext module resolution. Root tsconfig.json references all four packages in dependency order. vitest.config.ts discovers projects from packages/_ and apps/_. .gitignore covers all generated artifacts.
</done>
</task>

<task type="auto">
  <name>Task 2: Create all workspace packages with tsconfigs and stub source files</name>
  <files>
    packages/storage/package.json
    packages/storage/tsconfig.json
    packages/storage/src/index.ts
    packages/storage/vitest.config.ts
    packages/engine/package.json
    packages/engine/tsconfig.json
    packages/engine/src/index.ts
    packages/engine/vitest.config.ts
    packages/server/package.json
    packages/server/tsconfig.json
    packages/server/src/index.ts
    packages/server/vitest.config.ts
    apps/console/package.json
    apps/console/tsconfig.json
    apps/console/tsconfig.node.json
    apps/console/index.html
    apps/console/src/main.tsx
    apps/console/src/App.tsx
    apps/console/src/vite-env.d.ts
    apps/console/vitest.config.ts
    packages/python-sidecar/requirements.txt
    packages/python-sidecar/sandbox_runner.py
  </files>
  <action>
**packages/storage** — no dependencies on other workspace packages:
- `package.json`: `"name": "@trajectory/storage"`, `"version": "0.0.1"`, `"private": true`, `"type": "module"`, `"main": "./dist/index.js"`, `"types": "./dist/index.d.ts"`, `"exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } }`, `"scripts": { "build": "tsc --build" }`, no dependencies yet (better-sqlite3 comes in Phase 2)
- `tsconfig.json`: extends `../../tsconfig.base.json`, `"outDir": "./dist"`, `"rootDir": "./src"`, `"composite": true`, no references, include `["src/**/*"]`, exclude `["node_modules", "dist"]`
- `src/index.ts`: export a placeholder — `export const STORAGE_VERSION = '0.0.1' as const;`
- `vitest.config.ts`: `defineProject({ test: { name: 'storage', environment: 'node' } })`

**packages/engine** — depends on @trajectory/storage:

- `package.json`: `"name": "@trajectory/engine"`, same structure as storage. Add `"dependencies": { "@trajectory/storage": "*" }`
- `tsconfig.json`: same structure, add `"references": [{ "path": "../storage" }]`
- `src/index.ts`: `import { STORAGE_VERSION } from '@trajectory/storage'; export const ENGINE_VERSION = '0.0.1' as const;` — this verifies cross-package imports work
- `vitest.config.ts`: `defineProject({ test: { name: 'engine', environment: 'node' } })`

**packages/server** — depends on @trajectory/engine (and transitively storage):

- `package.json`: `"name": "@trajectory/server"`, same structure. Add `"dependencies": { "@trajectory/engine": "*" }`. Add `"devDependencies": { "@types/node": "^20.0.0" }`
- `tsconfig.json`: same structure, add `"references": [{ "path": "../engine" }]`
- `src/index.ts`: `import { ENGINE_VERSION } from '@trajectory/engine'; console.log('Trajectory Action Server', ENGINE_VERSION);` — placeholder, Express setup comes in Plan 02
- `vitest.config.ts`: `defineProject({ test: { name: 'server', environment: 'node' } })`

**apps/console** — independent React+Vite app, no workspace package dependencies:

- `package.json`: `"name": "@trajectory/console"`, `"version": "0.0.1"`, `"private": true`, `"type": "module"`, `"scripts": { "dev": "vite", "build": "tsc -b && vite build", "preview": "vite preview" }`, `"dependencies": { "react": "^19.0.0", "react-dom": "^19.0.0" }`, `"devDependencies": { "@vitejs/plugin-react": "^4.0.0", "vite": "^6.0.0", "@types/react": "^19.0.0", "@types/react-dom": "^19.0.0" }`
- `tsconfig.json`: extends `../../tsconfig.base.json`, overrides: `"target": "ES2020"`, `"module": "ESNext"`, `"moduleResolution": "Bundler"`, `"jsx": "react-jsx"`, `"noEmit": true`, `"composite": true`, `"verbatimModuleSyntax": false` (React imports are not type-only). Include `["src/**/*"]`
- `tsconfig.node.json`: for Vite config files — `"extends": "../../tsconfig.base.json"`, `"compilerOptions": { "composite": true, "module": "ESNext", "moduleResolution": "Bundler", "allowImportingTsExtensions": true, "noEmit": true, "verbatimModuleSyntax": false }`, include `["vite.config.ts"]`
- `index.html`: standard Vite React HTML shell with `<div id="root">` and `<script type="module" src="/src/main.tsx">`
- `src/main.tsx`: standard React 19 entry — `import { createRoot } from 'react-dom/client'; import App from './App'; createRoot(document.getElementById('root')!).render(<App />);`
- `src/App.tsx`: minimal component — `export default function App() { return <h1>Trajectory Management Console</h1>; }`
- `src/vite-env.d.ts`: `/// <reference types="vite/client" />`
- `vitest.config.ts`: `defineProject({ test: { name: 'console', environment: 'jsdom' } })`

**packages/python-sidecar** — NOT an npm workspace, just a directory:

- `requirements.txt`: empty file with comment `# Python sidecar dependencies — populated in Phase 3`
- `sandbox_runner.py`: placeholder with comment `# Entry point for Python sandbox execution — implemented in Phase 3`

IMPORTANT: Use `import { defineProject } from 'vitest/config'` (not `defineConfig`) in per-package vitest configs.
</action>
<verify>
Run from repo root:

```bash
npm install
npm run build
npx vitest run --reporter=verbose 2>&1 | head -20
```

- `npm install` must complete without errors and create `node_modules/@trajectory/` symlinks
- `npm run build` (`tsc --build`) must compile all packages without type errors
- `vitest run` must discover 4 projects (storage, engine, server, console) — 0 tests is fine
- Verify cross-package import works: `node --loader tsx packages/server/dist/index.js` or just check `tsc --build` succeeds (the engine importing from storage proves it)
  </verify>
  <done>
  Four workspace packages exist with correct package.json (scoped @trajectory/\* names, proper dependencies), tsconfigs (extending shared base, correct project references in dependency order), stub source files (with cross-package imports proving resolution works), and per-package vitest configs. Python sidecar directory exists at packages/python-sidecar/ with placeholder files. npm install links all workspaces. tsc --build compiles without errors. vitest discovers all 4 projects.
  </done>
  </task>

</tasks>

<verification>
Run these commands from the repo root after both tasks complete:

1. **Workspace linking**: `npm install` — must succeed, `ls node_modules/@trajectory/` shows server, engine, storage, console symlinks
2. **TypeScript build**: `npm run build` — must produce dist/ in packages/server, packages/engine, packages/storage with .js and .d.ts files
3. **Cross-package types**: Check that `packages/server/dist/index.d.ts` exists and `packages/engine/dist/index.d.ts` exists
4. **Vitest discovery**: `npx vitest run --reporter=verbose` — must list 4 projects (storage, engine, server, console), 0 test suites is acceptable
5. **Console app**: `ls apps/console/index.html apps/console/src/main.tsx apps/console/src/App.tsx` — all exist
6. **Python sidecar**: `ls packages/python-sidecar/requirements.txt packages/python-sidecar/sandbox_runner.py` — both exist
7. **No stale files**: `ls packages/python-sidecar/package.json 2>/dev/null` — must NOT exist (not an npm workspace)
   </verification>

<success_criteria>

- `npm install` succeeds and creates workspace symlinks at node_modules/@trajectory/\*
- `npm run build` compiles all TypeScript packages without errors
- `npx vitest run` discovers 4 projects (storage, engine, server, console)
- Cross-package imports resolve: engine imports from storage, server imports from engine
- Python sidecar directory exists at packages/python-sidecar/ with placeholder files
- No TypeScript errors, no missing type declarations
  </success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
